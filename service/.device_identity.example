# 小智設備資料設定範例 (.device_identity)
# ---------------------------------------------------------
# 這個檔案通常由程式自動產生，您不需要手動建立。
# 但如果您需要備份或遷移身分，可以參考此格式。
#
# 使用方式：
# 1. 複製此檔案並重新命名為 .device_identity
# 2. 填入您的設備資訊 (如果沒有，程式會自動生成新的)
# ---------------------------------------------------------

# 您的設備 MAC 地址 (格式：aa:bb:cc:dd:ee:ff)
# 如果留空或刪除此行，程式會自動生成一個隨機地址
device_mac=

# 您的客戶端 ID (通常與 MAC 地址相同)
client_id=
